{
  "name": "voiceshield-workspace",
  "version": "1.0.0",
  "description": "VoiceShield Workspace - Cybersecurity voice deepfake detection platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:ml": "cd ml-service && python -m uvicorn main:app --reload --port 8000",
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ml\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
